{
  "name": "template-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "dev:local": "miniflare --watch --modules src/index.js --host 0.0.0.0 --port 8787",
    "build": "wrangler build",
    "deploy": "wrangler deploy --env production",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "wrangler": "^4.0.0",
    "miniflare": "^4.20260623.0"
  },
  "dependencies": {}
}
